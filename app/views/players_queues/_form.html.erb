<%= form_with(model: players_queue) do |form| %>
  <% if players_queue.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(players_queue.errors.count, "error") %> prohibited this players_queue from being saved:</h2>

      <ul>
        <% players_queue.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :players, style: "display: block" %>
    <%= form.text_field :players %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :room_id, style: "display: block" %>
    <%= form.number_field :room_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
